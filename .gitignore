# Nix build symlinks
result
result-*

# direnv
.direnv/

# Server working dirs
*-data/

# Built local-mod jars: built by CI, published as GitHub Release assets,
# and pinned in pack/mods/*.pw.toml (see scripts/pin-local-mods.sh). Kept
# locally (uncommitted) as a dev-iteration override for scripts/build-mods.sh
# -- see the "Local mods" comment in flake.nix.
local-mods/*.jar


# trash
.Trash-1000/
