# Nix build symlinks
result
result-*

# direnv
.direnv/

# Server working dirs
*-data/

# local-mods/*.jar is deliberately NOT ignored: the jars are committed and are
# the single source of truth for the local mods. See the "Local mods" comment
# in flake.nix.


# trash
.Trash-1000/
